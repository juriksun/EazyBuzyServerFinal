//not now - maybe no need
'use strict';

const STATUS_ENUM = { READY : 0 , INACTIVE : 1 , FINISHED : 2 },
      PRIORITY_ENUM = { LOW : 0 , MEDIUM : 1 , HIGH : 2 } 


module.exports = class{
    

    getTask(task_id){
       // db.find({id:task_id})
    }
}